# A caculator that takes the inputted heights in cm of students and finds the average.
# Uses for-loop instead of sum() and len()

student_heights = input("Input a list of student heights ").split()
for n in range(0, len(student_heights)):
  student_heights[n] = int(student_heights[n])

total_height = 0
for height in student_heights:
    total_height += height


total_entries = 0
for entry in student_heights:
    total_students += 1


print(round(total_height / total_students))
